# yaml-language-server: $schema=../../../template.schema.json
id: dotnet-service/service
name: .NET Service Add Service
description: Adds a new service to the workspace
version: 1.0.0
language: csharp
steps:
  - action: render_folder
    source: 'content/'
    destination: '.'
